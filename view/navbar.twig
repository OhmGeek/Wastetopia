<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand hidden-xs" href="{{ config.ROOT_BASE }}">Wastetopia</a>
			<a class="navbar-brand visible-xs" href="{{ config.ROOT_BASE }}">WT</a>
			<form class="search-form navbar-left" onsubmit="return searchSubmit(event)">
				<div class="search-group">
					<div class="search">
						<input type="text" placeholder="Search" id="searchBox">
					</div>
					<button type="submit" value="Submit" class="btn-search">
						<i class="material-icons">search</i>
					</button>
				</div>
				<div class="postcode">
					<input type="text" placeholder="Postcode" id="postcodeBox">
				</div>
			</form>
		</div>
		<div class="navbar-collapse collapse navbar-responsive-collapse">
			<ul class="nav navbar-nav navbar-right">
				<li><a href="{{ config.ROOT_BASE }}/messages"><i class="material-icons">message</i>{% if header.isLoggedIn == 1 %}<span class="badge" id="messageNotifications"></span>{% endif %} <span class="visible-xs">Messages</span></a></li>
				<li><a href="{{config.ROOT_BASE}}/profile"><i class="material-icons">person</i>{% if header.isLoggedIn == 1 %} <span class="badge" id="requestNotifications"></span> {% endif %} <span class="visible-xs">Profile</span><</a></li>
			  {% if header.isLoggedIn == 1 %}
				<li><a href="{{config.ROOT_BASE}}/logout">Log out</a></li>
				{% else %}
				<li><a href="{{config.ROOT_BASE}}/login">Log in</a></li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
